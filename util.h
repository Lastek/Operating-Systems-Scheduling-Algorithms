#include <cmath>
#include <vector>

#include "types.h"
extern int double_cmp(double x, double y, double precision=1000);

extern std::vector<proc_data_t> lazysort_vec(const std::vector<proc_data_t> &v);